# Klotin - Desastres Naturales

App Android (Kotlin + Jetpack Compose), en reconstrucción desde cero.

## Estructura

- `app/` — módulo único de la aplicación.

## Desarrollo

```
./gradlew :app:assembleDebug
```
